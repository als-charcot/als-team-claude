# All data lives here, in one place — distributed separately, never committed
data/

# Researchers' own analyses live here and stay local (each can be its own repo)
projects/

# Python / Jupyter junk
__pycache__/
*.pyc
.ipynb_checkpoints/
.venv/

# OS junk
.DS_Store
Thumbs.db
